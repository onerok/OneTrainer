# Web Apps

This directory contains the new web stack for OneTrainer:

- `apps/api`: FastAPI backend
- `apps/web`: Svelte frontend (shadcn-style component architecture)

Current implementation scope: `General` tab only.
